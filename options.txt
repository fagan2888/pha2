# To use: 
# "python get_scenario_data.py --write-rhos" to create inputs dir, basic inputs and rho files in all inputs dirs
# "python write_wwph_suffixes.py" to create suffixes files in all inputs dirs
# Run the following command twice with different input_dir variables to create pha and pha_mean subdirs and scenario data
# "python get_pha_data_monthly_logistic.py"
# "cp inputs/pha_125_logistic_mean/fuel_cost_curves.tab inputs"
# "cp inputs_tiny/pha_125_logistic_mean/fuel_cost_curves.tab inputs_tiny"
# create root and tree .dat files in pha and pha_mean subdirs:
# "python ReferenceModel.py --inputs-dir inputs --pha-subdir pha_125_logistic"
# "python ReferenceModel.py --inputs-dir inputs_tiny --pha-subdir pha_125_logistic"
# use runph with various options to solve the model (note: it will not use command line arguments
# so all model configuration must be done with options.txt or the USE_PSIP_PLAN environment variable)
# use summarize_scenario_results_01_99.py or summarize_scenario_results.py to get statistics 
# for all the model runs

--inputs-dir inputs
--pha-subdir pha_125_logistic
# must be set when running ReferenceModel.py to create ScenarioStructure.dat
--pha-scenario-count 125
--demand-response-share 0.103
--biofuel-limit 1.0
--psip-relax
--verbose 
# note: following options are only used by 'switch solve', not runph (pha)
--stream-solver
--solver=cplex --solver-io=nl 
#--solver-options-string="mipgap=0.001 display=1 mipdisplay=1 primalopt='' advance=2 threads=1 iisfind=1"
--solver-options-string="mipgap=0.001 display=1 mipdisplay=1 primalopt='' advance=2 threads=1"
--suffixes iis
